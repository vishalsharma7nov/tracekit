# tracekit_lints

Recommended analysis options for TraceKit consumers.

Add to your `analysis_options.yaml`:

```yaml
include: package:tracekit_lints/lib/analysis_options.yaml
```

This enables `avoid_print` and documents the TraceKit logging conventions.
